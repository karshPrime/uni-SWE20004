int main() {
  int arr[10]; // declaring array of size 10
// by default all elements are set to 0
// hence no point in declaring again with all elements as 0
  arr[0] = 100; // overwriting first value as 100
  arr[9] = 1000; // overwriting last value as 1000
}
